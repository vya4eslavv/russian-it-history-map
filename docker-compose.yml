version: '3.9'
services:
  postgres:
    image: postgres:latest
    environment:
      - POSTGRES_USER=slava
      - POSTGRES_PASSWORD=slava
      - POSTGRES_DB=postgres
    ports:
      - "1488:5432"
    restart: unless-stopped