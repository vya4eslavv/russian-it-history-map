# История IT в СССР и России

> Интерактивный веб-проект о ключевых отечественных IT- и инженерных разработках, связанных с регионами России.

![Главный экран](../image.png)

## О проекте

**«История IT в СССР и России»** — это интерактивная карта, которая показывает вклад разных регионов в развитие вычислительной техники, программирования, микроэлектроники и цифровых технологий. Пользователь может выбрать регион на карте и посмотреть связанные с ним достижения, описания, изображения и исторический контекст.

Проект задуман как **образовательный и популяризаторский ресурс** для школьников, студентов и всех, кто интересуется историей отечественных технологий. Эта цель прямо сформулирована и в сопровождающем тезисе проекта: сайт с интерактивной картой должен предоставлять структурированную информацию о технических достижениях конкретного региона и повышать интерес молодежи к отечественным инженерным и информационным технологиям. fileciteturn0file0L1-L25

---

## Что умеет сайт

- 🗺️ **Интерактивная карта России** с выбором региона
- 🏛️ **Карточки достижений** по конкретному региону
- 🧠 **Исторические описания** отечественных IT-разработок
- 🎮 **Квиз** по истории IT в СССР и России
- 🧩 Дополнительные тематические страницы:
  - ИТМО
  - «Собери ЭВМ»
  - «Эмулятор ЭВМ»
  - Достижения
  - О проекте
- 🐘 **Backend API** для получения достижений по коду региона
- 🐳 **Docker Compose-запуск** всего проекта одной командой

![Квиз](../2026-04-06_16-51-07.png)

---

## Технологический стек

### Frontend
- HTML5
- CSS3
- JavaScript (vanilla)
- SVG-карта России
- Nginx для раздачи статических файлов

### Backend
- Go
- Gorilla Mux
- Gorilla Handlers (CORS)
- sqlx

### Database
- PostgreSQL 16
- SQL-инициализация схемы и seed-данных через `db/init`

### Infra
- Docker
- Docker Compose

---

## Архитектура проекта

```text
.
├── backend/
│   └── history_project/
│       ├── app/
│       │   ├── handlers/
│       │   ├── models/
│       │   ├── services/
│       │   └── main.go
│       ├── Dockerfile
│       ├── go.mod
│       └── go.sum
├── db/
│   └── init/
│       ├── 001_schema.sql
│       ├── 002_seed.sql
│       ├── 003_achievement_images.sql
│       └── 004_full_descriptions.sql
├── frontend/
│   ├── quiz.html
│   ├── achievements.html
│   ├── emulator.html
│   ├── build-evm.html
│   ├── itmo.html
│   ├── about-project.html
│   └── style.css
├── images/
├── js/
├── index.html
├── frontend-nginx.conf
└── docker-compose.yml
```

---

## Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <your-repo-url>
cd russian-it-history-map-master
```

### 2. Проверка `.env`

В проекте используется файл `.env` с параметрами PostgreSQL. Убедитесь, что в нем есть такие переменные:

```env
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=history_map
```

### 3. Запуск проекта

```bash
docker-compose up --build
```

### 4. Открыть в браузере

Фронтенд будет доступен по адресу:

```text
http://0.0.0.0:80
```

Обычно локально удобнее открывать и так:

```text
http://localhost:80
```

---

## Docker-сервисы

После запуска поднимаются 3 контейнера:

### `postgres`
- PostgreSQL 16
- порт: `1488:5432`
- автоматически применяет SQL-скрипты из `db/init`

### `backend`
- Go API
- порт: `8080:8080`
- подключается к БД по внутреннему имени сервиса `postgres`

### `frontend`
- Nginx
- порт: `80:80`
- раздает статические страницы проекта

---

## API

### Получить достижения региона

```http
GET /api/regions/{regionCode}/achievements
```

### Пример

```bash
curl http://localhost:8080/api/regions/RU-MOW/achievements
```

### Пример ответа

```json
{
  "region": {
    "code": "RU-MOW",
    "name": "Москва"
  },
  "inventions": [
    {
      "id": 1,
      "slug": "agat",
      "name": "Агат",
      "inventor": "",
      "period": "1981–1983; выпуск примерно 1984–1990",
      "location_name": "Москва",
      "description": "8-битный персональный компьютер...",
      "image_url": "/images/final/...",
      "full_description": "..."
    }
  ]
}
```

---

## SQL-инициализация

При первом запуске база подготавливается автоматически:

- `001_schema.sql` — схема БД
- `002_seed.sql` — стартовые данные
- `003_achievement_images.sql` — изображения достижений
- `004_full_descriptions.sql` — расширенные описания

Это позволяет сразу поднять проект с готовым демонстрационным наполнением.

---

## Пользовательские сценарии

### Главная страница
Пользователь видит большую интерактивную карту России и может выбрать интересующий регион, чтобы узнать о локальных IT-достижениях.

### Квиз
Проверка знаний по истории IT в СССР и России с выбором уровня сложности, прогресс-баром и пояснениями к ответам.

### Тематические страницы
Дополнительные страницы рассказывают о проекте, достижениях, ИТМО и позволяют взаимодействовать с историей вычислительной техники в более игровом формате.

---

## Для кого этот проект

- для студентов и школьников
- для преподавателей
- для организаторов выставок и образовательных мероприятий
- для всех, кто интересуется историей отечественных технологий

---

## Возможности для развития

Согласно описанию проекта, в будущем планируется расширение платформы: добавление механизма, через который вузы и технические организации смогут самостоятельно пополнять базу знаний проекта новыми материалами и достижениями. fileciteturn0file0L20-L25

Что можно развить технически:

- административную панель для добавления материалов
- поиск по достижениям, персоналиям и временным периодам
- фильтрацию по типу разработки
- отдельные страницы персоналий
- таймлайн по эпохам
- мультиязычность
- адаптацию под мобильные устройства
- тесты для backend и frontend

---

## Полезные команды

### Остановить проект

```bash
docker-compose down
```

### Остановить проект с удалением volume БД

```bash
docker-compose down -v
```

### Пересобрать контейнеры

```bash
docker-compose up --build --force-recreate
```

### Посмотреть логи

```bash
docker-compose logs -f
```

---

## Почему проект выглядит необычно

Визуальный стиль проекта опирается на ретро-эстетику: темные фоны, контрастные красные акценты, пиксельный шрифт и крупные интерактивные блоки. Это хорошо сочетается с темой истории вычислительной техники и делает проект визуально запоминающимся.

---

## Авторский контекст

Проект связан с Университетом ИТМО и представлен как интерактивная карта отечественных разработок в области технологий. Это зафиксировано в тезисе авторов Пивнева З. О., Еремина В. А. и Прокофьева Е. С. fileciteturn0file0L1-L12

---

## License

Укажите здесь выбранную лицензию проекта, например:

```text
MIT
```

или

```text
All rights reserved
```

---

## TL;DR

```bash
docker-compose up --build
```

Открыть:

```text
http://0.0.0.0:80
```

---

Если захочешь, я могу сразу сделать еще и **короткую GitHub-версию README**, более лаконичную и «продающую» для репозитория.
